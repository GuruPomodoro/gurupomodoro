<nav class="navbar navbar-inverse" id="header">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="/">
          <%= image_tag("Glogo.png", :alt => "logo") %>
        </a>
      </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                    <% if current_team.present? %>
                        <%= current_team.title %>
                    <% else %>
                        Team Select
                    <% end %>
                    <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <% current_user.teams.each do |team| %>
                            <li><%= link_to(team.title, team) %></li>
                        <% end %>
                        <li role="separator" class="divider"></li>
                      <li><%= link_to("Create New Team", new_team_path) %></li>

                    </ul>
                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <% if current_team.present? %>
                    <li><%= link_to("Team Dashboard", current_team ) %></li>
                    <li><%= link_to("Start Pomodoro", pomodoro_start_path, class: 'start-pomodoro' ) %></li>
                <% end %>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <%= current_user.full_name %>
                    <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><%= link_to("Show Reports", personalreport_path) %></li>
                        <li><%= link_to("Edit Profile", edit_user_registration_path) %></li>
                        <li><%= link_to('Sign Out', destroy_user_session_path, method: :delete) %></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
